<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.doby.web.repository.SmalltalkTagDefineRepository">

    <select id="findAll" resultType="SmalltalkTagDefine">
        select * from smalltalk_tag_define where name like '%${query}%'
    </select>

</mapper>